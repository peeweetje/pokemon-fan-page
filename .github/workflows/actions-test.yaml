name: Actions Test

on:
  push:
    branches:
      - add-workflow-using-github-actions
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install

      - name: Set up Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Push changes using token
        if: github.ref == 'refs/heads/add-workflow-using-github-actions'
        env:
          token: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git remote set-url origin https://x-access-token:${token}@github.com/${{ github.repository }}.git
          # Example git commands (uncomment and modify as needed):
          # git add .
          # git commit -m "Automated commit"
          # git push origin add-workflow-using-github-actions
